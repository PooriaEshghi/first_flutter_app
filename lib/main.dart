import 'package:flutter/material.dart';

import 'http-request/myApp.dart';

void main() => runApp(MaterialApp(
    title: 'demo',
    theme: ThemeData(primarySwatch: Colors.deepOrange),
    home: const MyApp()));
